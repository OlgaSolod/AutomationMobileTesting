Создала метод:
private void checkOrientation(ScreenOrientation orientation) {
        if (orientation != ScreenOrientation.PORTRAIT) {
            driver.rotate(ScreenOrientation.PORTRAIT);
        }
    }
    
И в каждом тесте этот метод я помещаю перед всеми другими методами и передаю туда параметр driver.getOrientation():
@Test
    public void returningAppFromBackground() {
        checkOrientation(driver.getOrientation());

        waitForElementAndClick(By.id("org.wikipedia:id/search_container"),
                "Cannot find an element 'Search_Wikipedia' on the page",
                5);

        waitForElementAndSendKeys(
                By.id("org.wikipedia:id/search_src_text"),
                "Java",
                "Cannot find search input.",
                15
        );
        waitForElementPresent(
                By.xpath("//android.widget.TextView[@text='Java (programming language)']"),
                "Cannot find 'Java (programming language)' text on the page",
                5
        );
        driver.runAppInBackground(2);
        waitForElementPresent(
                By.xpath("//android.widget.TextView[@text='Java (programming language)']"),
                "Cannot find 'Java (programming language)' text on the page after returning from background",
                5
        );
    }
